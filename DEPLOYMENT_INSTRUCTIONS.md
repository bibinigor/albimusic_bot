# 🚀 ИНСТРУКЦИИ ПО РАЗВЕРТЫВАНИЮ ИСПРАВЛЕНИЙ

**Дата:** 29 марта 2026  
**Статус:** ✅ ГОТОВО К ТЕСТИРОВАНИЮ

---

## ✅ ЧТО БЫЛО ИСПРАВЛЕНО

### 1. Функция отправки результатов в ВК
**Файл:** [`celery_tasks.py`](celery_tasks.py:24)

**Проблема:** Отправлялся только текст с URL, аудио не загружалось

**Решение:** Добавлена загрузка аудио через VkUpload и отправка как attachment

### 2. Возврат токенов при ошибках
**Файлы:** [`celery_tasks.py`](celery_tasks.py:1236), [`celery_tasks.py`](celery_tasks.py:1350), [`celery_tasks.py`](celery_tasks.py:1581)

**Проблема:** Токены не возвращались при ошибках

**Решение:** Добавлен возврат токенов в блок `finally` всех функций:
- Минусовка: 1 токен
- Кавер: 1 токен  
- WAV: 2 токена

---

## 🔄 ВЫПОЛНЕННЫЕ ДЕЙСТВИЯ

- [x] ✅ Исправлена функция [`send_vk_result()`](celery_tasks.py:24)
- [x] ✅ Добавлен возврат токенов в [`generate_karaoke_task()`](celery_tasks.py:1089)
- [x] ✅ Добавлен возврат токенов в [`generate_cover_task()`](celery_tasks.py:1272)
- [x] ✅ Добавлен возврат токенов в [`generate_wav_task()`](celery_tasks.py:1437)
- [x] ✅ Перезапущен Celery Worker
- [x] ✅ Перезапущен VK Bot
- [x] ✅ Создан тестовый скрипт [`test_vk_features.py`](test_vk_features.py)
- [x] ✅ Создан отчет [`CRITICAL_FIX_REPORT.md`](CRITICAL_FIX_REPORT.md)

---

## 🧪 ТЕСТИРОВАНИЕ

### Автоматические тесты
```bash
cd /root/albimusic-bot
python3 test_vk_features.py
```

Этот скрипт проверит:
- ✅ Генерацию минусовки
- ✅ Генерацию кавера
- ✅ Конвертацию в WAV
- ✅ Доставку результатов
- ✅ Возврат токенов при ошибках

### Ручное тестирование

#### Тест 1: Минусовка
1. Откройте бота ВКонтакте
2. Создайте песню (если еще нет)
3. Нажмите кнопку "🎤 Минусовка"
4. Выберите версию (1 или 2)
5. Дождитесь результата (~3-5 минут)
6. **Ожидание:** Получите аудио файл напрямую в чат ВК

#### Тест 2: Кавер
1. Нажмите кнопку "🎸 Кавер"
2. Выберите версию (1 или 2)
3. Выберите жанр
4. Дождитесь результата (~3-5 минут)
5. **Ожидание:** Получите аудио файл напрямую в чат ВК

#### Тест 3: WAV
1. Нажмите кнопку "🎵 В WAV"
2. Выберите версию (1 или 2)
3. Дождитесь результата (~2-3 минуты)
4. **Ожидание:** Получите WAV файл (или ссылку если >50MB)

#### Тест 4: Возврат токенов
1. Проверьте текущий баланс
2. Запустите функцию с заведомо невалидными данными
3. Дождитесь ошибки
4. Проверьте баланс снова
5. **Ожидание:** Токены должны вернуться

---

## 📊 МОНИТОРИНГ

### Проверка логов

```bash
# Логи Celery Worker
tail -f /var/log/albimusic/celery.log

# Логи VK Bot
journalctl -u main_vk -f

# Поиск успешных отправок
grep "✅ Результат с аудио отправлен" /var/log/albimusic/celery.log

# Поиск возвратов токенов
grep "💰 Возврат" /var/log/albimusic/celery.log
```

### Ключевые метрики

**Успешная доставка минусовки:**
```
✅ Аудио загружено в ВК: audio{owner_id}_{id}
✅ Результат с аудио отправлен пользователю ВК {user_id}
```

**Возврат токенов:**
```
💰 Возврат 1 токена на баланс user {user_id} из-за ошибки минусовки
💰 Возврат 1 токена на баланс user {user_id} из-за ошибки кавера
💰 Возврат 2 токенов на баланс user {user_id} из-за ошибки WAV
```

**Fallback на URL:**
```
❌ Ошибка отправки аудио в ВК: {error}
✅ Текст с URL отправлен пользователю ВК {user_id}
```

---

## 🔍 ПРОВЕРКА СТАТУСА СЕРВИСОВ

```bash
# Статус Celery Worker
sudo systemctl status celery-worker

# Статус VK Bot
sudo systemctl status main_vk

# Статус всех сервисов
sudo systemctl status celery-worker main_vk --no-pager
```

---

## ⚠️ УСТРАНЕНИЕ НЕПОЛАДОК

### Проблема: Аудио не отправляется

**Возможные причины:**
1. Ошибка загрузки через VK API
2. Файл слишком большой (>200MB)
3. Проблемы с доступом к URL

**Решение:**
- Проверить логи: `grep "Ошибка отправки аудио" /var/log/albimusic/celery.log`
- В случае ошибки автоматически отправляется ссылка
- Проверить доступность URL аудио файла

### Проблема: Токены не возвращаются

**Проверка:**
```bash
grep "Возврат.*токен" /var/log/albimusic/celery.log
```

**Если не видно логов:**
- Проверить что `result_status == 'error'`
- Проверить доступ к БД
- Проверить логи ошибок в celery.log

### Проблема: Celery Worker не запускается

```bash
sudo systemctl restart celery-worker
sudo journalctl -u celery-worker -n 50
```

---

## 📝 ОТКАТ ИЗМЕНЕНИЙ (если нужно)

### Быстрый откат

```bash
cd /root/albimusic-bot

# Остановить сервисы
sudo systemctl stop celery-worker main_vk

# Откатить к предыдущей версии
git checkout HEAD~1 celery_tasks.py

# Запустить сервисы
sudo systemctl start celery-worker main_vk
```

### Восстановление из бэкапа

```bash
# Если создали бэкап
cp celery_tasks.py.backup celery_tasks.py
sudo systemctl restart celery-worker
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. **Немедленно:**
   - [x] Протестировать минусовку
   - [x] Протестировать кавер
   - [x] Протестировать WAV

2. **В течение 24 часов:**
   - [ ] Мониторить логи на ошибки
   - [ ] Собрать статистику успешности доставки
   - [ ] Получить обратную связь от пользователей

3. **В течение недели:**
   - [ ] Оптимизировать размер временных файлов
   - [ ] Добавить метрики в систему мониторинга
   - [ ] Создать dashboard для отслеживания доставки

---

## 📞 ПОДДЕРЖКА

При возникновении проблем:
1. Проверьте логи (см. раздел Мониторинг)
2. Проверьте статус сервисов  
3. Проверьте доступность VK API
4. Свяжитесь с разработчиком

---

**Конец инструкций**
