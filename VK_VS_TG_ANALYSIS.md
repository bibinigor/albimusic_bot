# Анализ различий между VK и Telegram ботами

## КРИТИЧЕСКИЕ ПРОБЛЕМЫ В VK БОТЕ:

### 1. **Минусовка (karaoke)**

**TELEGRAM (ПРАВИЛЬНО):**
- ✅ Спрашивает выбор версии (v1/v2)
- ✅ Списывает 1 токен ДО генерации
- ✅ Асинхронный вызов: `generate_karaoke_task.apply_async(args=[user_id, task_id, version], kwargs={'task_id': new_task_id}, queue='generation')`
- ✅ Не ждет результата синхронно

**VK (НЕПРАВИЛЬНО):**
- ❌ НЕ спрашивает версию - всегда версия 0
- ❌ Списывает токен ПОСЛЕ генерации
- ❌ Синхронный вызов: `.get(timeout=300)` - блокирует поток!
- ❌ Неправильные параметры: `args=(user_id, original_task_id, 0, None)` - лишний None
- ❌ Отсутствует `kwargs={'task_id': new_task_id}`

### 2. **WAV конвертация**

**TELEGRAM (ПРАВИЛЬНО):**
- ✅ Спрашивает выбор версии (v1/v2)
- ✅ Списывает 1 токен ДО конвертации
- ✅ Асинхронный вызов: `generate_wav_task.apply_async(args=[user_id, task_id, version], kwargs={'task_id': new_task_id}, queue='generation')`
- ✅ Не ждет результата синхронно

**VK (НЕПРАВИЛЬНО):**
- ❌ НЕ спрашивает версию - всегда версия 0
- ❌ Списывает 2 токена ПОСЛЕ конвертации
- ❌ Синхронный вызов: `.get(timeout=300)` - блокирует поток!
- ❌ Неправильные параметры: `args=(user_id, original_task_id, 0, None)` - лишний None
- ❌ Отсутствует `kwargs={'task_id': new_task_id}`

### 3. **Кавер**

**TELEGRAM (ПРАВИЛЬНО):**
- ✅ Спрашивает выбор версии (v1/v2)
- ✅ Списывает 2 токена ДО генерации
- ✅ Сохраняет версию в FSM
- ✅ Показывает выбор жанра
- ✅ Асинхронный вызов с правильными параметрами

**VK (НЕПРАВИЛЬНО):**
- ❌ НЕ спрашивает версию - всегда версия 0
- ❌ Списывает 1 токен ПОСЛЕ генерации (должно быть 2!)
- ❌ Синхронный вызов: `.get(timeout=300)` - блокирует поток!
- ❌ Неправильные параметры: `args=(user_id, original_task_id, genre, 0, None)` - лишний None
- ❌ Отсутствует `kwargs={'task_id': new_task_id}`

## ПЛАН ИСПРАВЛЕНИЯ:

1. Добавить выбор версии через inline-кнопки ВКонтакте
2. Изменить порядок: списывать токены ДО генерации
3. Убрать синхронное ожидание .get(timeout=300)
4. Исправить параметры Celery задач
5. Кавер должен стоить 2 токена (как в Telegram)
6. Использовать `kwargs={'task_id': new_task_id}` и `queue='generation'`
